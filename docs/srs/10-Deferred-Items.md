# Deferred Items — cross-stitch-platform documentation

**Status:** Living document — append to this, don't let deferred decisions get lost across
sessions.
**Date started:** 2026-07-11

This tracks documentation/decisions explicitly deferred during the `docs/srs/` effort —
things that came up, were judged out of scope for now, and need a real decision (not more
reverse-engineering) before they can move forward. Unlike the rest of `docs/srs/`, this
file is not "as-built" documentation — it's a parking lot.

## Open items

| Item | Deferred on | Why deferred | What's needed to unblock |
|---|---|---|---|
| **Data Migration Plan** | 2026-07-11 | Every other `docs/srs/` document reverse-engineers *current* behavior; a migration plan requires deciding the *target* state for known data debt (`00-Overview.md` §6, `05-SAD.md` §5.2): hash+salt passwords, reconcile the two user tables, canonicalize the Pinterest pin-ID attribute, add schema versioning. Olga: "не хочу пока ничего менять" — no appetite yet to commit to any of these changes. | Olga decides, per item, whether/when to actually change it. Once even one item has a real target state, that item alone can get a migration plan — doesn't need to wait for all of them. |
| **Backup and Disaster Recovery Plan** | 2026-07-11 | No existing backup/DR strategy was found or mentioned anywhere across the whole `docs/srs/` research effort (no DynamoDB point-in-time recovery, S3 versioning, or restore procedure referenced in any component). Writing this doc now would mean inventing a plan from nothing, not documenting one — and it needs RTO/RPO targets, which are a business decision, not something derivable from code. | Olga decides: (a) whether backup/DR is worth investing in given the single-operator scale of this platform, and if so (b) what RTO/RPO is acceptable, before a plan can be written meaningfully. |
| **CI (partial) built 2026-07-12 — extend as tests grow** | 2026-07-12 | A lightweight GitHub Actions workflow (`.github/workflows/web-ci.yml`) now runs `build` + `test` (Vitest) for `web/` on every push/PR — see `09-Test-Plan.md` §4.4. Deliberately **not** extended to: Playwright e2e (needs `playwright.config.ts`'s hardcoded Windows Chrome path resolved — Windows runner or bundled Chromium first), `pinterest-agent`/`autopinner`/`Uploader` (no tests exist there yet — a CI job with nothing to run adds no value), or any deployment step (deploys stay manual by design, per `11-Deployment-Guide.md` §2). | Add a new job the moment a component gets its first real test — per Olga's stated plan to write tests for the rest of the codebase. Revisit the Playwright-in-CI question separately once the Chrome-path issue is addressed. |
| **Legacy `CrossStitchItems` USER-row migration** | 2026-07-11 | Verified by direct DynamoDB count: 57,537 legacy `EntityType="USER"` rows in `CrossStitchItems` vs. only 1,561 rows in the modern `CrossStitchUsers` table — the legacy table is the dominant data source, not residual cruft (`08-Data-Dictionary.md` §1.3/§2). Olga: this is a known artifact from before the modern table existed; the plan is to check which of those old emails are still live and migrate them into the modern format. | Olga to audit which legacy emails are still active/valid, then decide the actual migration mechanics (bulk script, mapping of legacy fields like `OpenPwd`/`cid` to the modern schema, whether email verification re-runs for migrated accounts). Feeds into the Data Migration Plan above once scoped. |
| **`SubscriptionEvents` table doesn't exist** | 2026-07-11 | Verified: `web/src/lib/subscription-events.ts`'s `recordSubscriptionEvent()` calls `PutItemCommand` against a table that isn't provisioned in DynamoDB (`DescribeTableCommand` → `ResourceNotFoundException`). The call is wrapped in try/catch and fails silently (`console.error`, returns `null`) — no impact on the subscription-confirm or PayPal-webhook flows that call it, but zero audit rows have ever been written. Olga: PayPal/paid-mode monetization isn't actually live yet ("думаю ввести PayPal в будущем") — this table's absence is consistent with that feature not having been activated, not a live bug losing real data. | Once Olga activates paid-mode/PayPal monetization for real: provision the `SubscriptionEvents` table (or reuse `DDB_SUBSCRIPTION_EVENTS_TABLE` env var to point it at whatever table name is actually created — see `14-Configuration-Reference.md` §1.5) before going live, so the audit trail actually captures events from day one instead of silently no-oping. |
| **Pinterest pin-ID attribute has six historical spellings** | 2026-07-12 | `00-Overview.md` §5 documents the data-quality issue (`PinID`, `PinId`, `PinterestPinId`, `PinterestPinID`, `PinterestID`, `PinterestId` on `CrossStitchItems` design rows). Investigated in detail this session: root cause is no shared-schema enforcement — each component (legacy code, Uploader, autopinner) wrote its own field name independently over time, with nothing to catch drift until a reader broke. Only two independent readers do the six-way fallback check today (`automation/autopinner/src/AutoPinner/DynamoDbDesignRepository.cs`'s `PinIdAttributeNames`/`HasAnyPinId()`/`ReadFirstPinIdOrNull()`, and `web/src/lib/data-access.ts:186-193`); every writer (Uploader, autopinner) already writes only the canonical `PinID`. Table is small (~5,260-5,500 design rows per `_check_table_sizes.ts`), so a full scan+rewrite is cheap and low-risk. Unlike the bundled Data Migration Plan item above, this one already has a concrete target state and low-risk scope — it doesn't need to wait for the others. | Olga to greenlight: (1) a backfill script, following the existing `backfill-*.ts` pattern in `automation/pinterest-agent/scripts/` (`--dry-run` first, idempotent per-item `UpdateCommand`), that resolves each row's first non-empty legacy spelling, writes canonical `PinID`, and removes the five legacy attributes; (2) then simplify the two readers above to check only `PinID`, dropping the six-way fallback. |

## Resolved items

*(none yet — move an item here, with the resolution and date, once it's actually decided
and either written up or explicitly dropped)*
